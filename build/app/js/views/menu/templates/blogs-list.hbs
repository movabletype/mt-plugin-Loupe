<div class="histories">
  {{#if historiesLoading}}
  <div class="loading"></div>
  {{/if}}
  {{#if histories.length}}
    <ul class="histories-list">
    {{#each histories}}
      <li data-id="{{id}}" class="blog-item{{#if this.selected}} selected{{/if}}">
        <a href="#" data-id="{{id}}">
          <span class="circle blog-asset"><span class="circle-inner"></span></span>
          <span class="blog-name">{{this.name}}</span>
        </a>
      </li>
    {{/each}}
    </ul>
  {{/if}}
</div>
<div class="blogs">
  {{#if blogsLoading}}
  <div class="loading"></div>
  {{/if}}
  <ul>
  {{#if error}}
    <li class="blog-item error">
      {{error.message}}
    </li>
  {{/if}}
  {{#each blogs}}
    <li data-id="{{id}}" class="blog-item{{#if this.selected}} selected{{/if}}">
      <a href="#" data-id="{{id}}">
        <span class="circle blog-asset"><span class="circle-inner"></span></span>
        <span class="blog-name">{{this.name}}</span>
      </a>
    </li>
  {{/each}}
    <li class="user-info">
      {{#if user}}
      {{#with user}}
      <a href="#logout">
        {{#if this.userpicUrl}}
          <div class="circle userpic" style="background-image: url({{userpicUrl}}); -pie-background: url({{userpicUrl}}) no-repeat center center / cover"></div>
        {{else}}
          <div class="userpic no-image icon-user"></div>
        {{/if}}
        <div class="display-name">{{#trans ../trans}}Logout{{/trans}}</div>
      </a>
      {{/with}}
      {{/if}}
    </li>
  </ul>
</div>
<div class="blog-item-nav-container">
{{#ifIs prev exists=true}}
  <div class="blog-item-nav blog-item-nav-prev" data-offset="{{prev}}"><span class="icon-arrow-left"></span>{{#trans trans}}previous{{/trans}}</div>
{{/ifIs}}
{{#ifIs totalResults gt=next}}
  <div class="blog-item-nav blog-item-nav-next" data-offset="{{next}}">{{#trans trans}}next{{/trans}}<span class="icon-arrow"></span></div>
{{/ifIs}}
</div>
