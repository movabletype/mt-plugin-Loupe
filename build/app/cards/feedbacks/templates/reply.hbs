{{#if entryIsPublished}}
{{#if commentIsApproved}}
  {{>js_views_card_partials_loading}}
  {{#if initial}}
  <div id="reply-container">
    <div class="reply-button-container">
      <div id="reply-button" class="button">{{#trans trans}}Reply{{/trans}}</div>
    </div>
  </div>
  {{else}}
  {{#if replied}}
  <div id="reply-container">
    <div class="reply-button-container">
      <div id="reply-button" class="button">{{#trans trans}}Another comment{{/trans}}</div>
    </div>
  </div>
  <div id="reply-textarea-container">
    <div id="reply-complete" class="ok contents-top-button button">
      <span class="button-icon icon-checkmark-circle"></span>
      <span id="accepted" class="button-label">{{#trans trans}}Posted comment successfully{{/trans}}</span>
    </div>
    <div>{{body}}</div>
  </div>
  {{/if}}
  {{#if form}}
  <div id="reply-container">
    <div class="reply-button-container">
      <div id="reply-disabled-button" class="button button-disabled">{{#trans trans}}Reply{{/trans}}</div>
    </div>
  </div>
  <div id="reply-textarea-container">
    {{#if error}}
    <div class="error">
      <span class="message"><span class="message-inner">{{#trans trans}}{{error}}{{/trans}}</span></span>
    </div>
    {{/if}}
    <textarea id="reply-textarea">{{body}}</textarea>
    <div id="do-reply-button" class="button">{{#trans trans}}Post the reply{{/trans}}</div>
  </div>
  {{/if}}
  {{/if}}
{{/if}}
{{/if}}
